DataSourceFor(key_path) = {key_path, function extract( file, query, setting)}

objects = {}

for each key in the object{
  insert (element path for this key, root) to Task["build"][i]
  DataSourceFor[key_path] = getDataExtractor (root, element path for this key, {attributes){return extract function()}
  if relationship found{
    Task["relationship"][i]= (key_path 1, key_path 2, relation){return function for relation}
  }
}

for each path in DataSourceFor[*]{
  data[key_path] = DataSourceFor[i].extractfuntion(){return data}

}

for each relation in Task["relationship"]{
  run relation functiuon (data[key_path1], data[key_path2])

}

for each path in Task["build"]{
  var newObject = new object
  putdata(path , newObject) //
  dataFinal[root].push(newObject)

}
